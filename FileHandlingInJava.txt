Java has several types of file handlers (classes for handling file I/O), grouped based on how they operate:

---

### 🔹 1. **Character Streams** (for text files)

* Read/write **characters** (`char`)
* Classes:

  * `FileReader` – read characters
  * `FileWriter` – write characters
  * `BufferedReader` – efficient reading with `readLine()`
  * `BufferedWriter` – efficient writing
  * `PrintWriter` – convenient writing (formatted, like `System.out`)

---

### 🔹 2. **Byte Streams** (for binary files)

* Read/write **bytes** (`byte`)
* Classes:

  * `FileInputStream` – read raw bytes
  * `FileOutputStream` – write raw bytes
  * `BufferedInputStream` / `BufferedOutputStream` – efficient byte I/O
  * `DataInputStream` / `DataOutputStream` – read/write Java primitives

---

### 🔹 3. **NIO (New I/O) – `java.nio.file` Package**

* More modern, powerful, and flexible (Java 7+)
* Classes/Interfaces:

  * `Path` – represents a file/directory path
  * `Files` – utility class for file operations (`readString`, `write`, `copy`, etc.)
  * `FileChannel` – for fast I/O and memory-mapped files
  * `ByteBuffer` / `CharBuffer` – low-level buffers

---

### 🔹 4. **Random Access Files**

* Read/write from **any position** in a file
* Class:

  * `RandomAccessFile`

---

### 🔹 5. **Object Streams**

* For reading/writing Java objects (serialization)
* Classes:

  * `ObjectInputStream`
  * `ObjectOutputStream`

---

### ✅ Summary Table

| Type              | Common Classes                                               | Use Case                         |
| ----------------- | ------------------------------------------------------------ | -------------------------------- |
| Character Streams | `FileReader`, `FileWriter`, `BufferedReader`                 | Text files                       |
| Byte Streams      | `FileInputStream`, `FileOutputStream`, `BufferedInputStream` | Binary files (images, audio)     |
| NIO               | `Files`, `Path`, `FileChannel`, `ByteBuffer`                 | Modern, scalable file handling   |
| Random Access     | `RandomAccessFile`                                           | Seek/read/write anywhere in file |
| Object Streams    | `ObjectInputStream`, `ObjectOutputStream`                    | Java object serialization        |

---

Let me know if you want code samples or comparisons.
